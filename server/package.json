{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --no-env-file --watch src/index.js",
    "start": "bun --no-env-file src/index.js",
    "test": "bun --no-env-file test",
    "redis:check": "bun --no-env-file scripts/check-redis.js",
    "benchmark": "bun --no-env-file scripts/benchmark-hot-path.js",
    "benchmark:http": "bun --no-env-file scripts/benchmark-http.js",
    "latency:report": "bun --no-env-file scripts/report-latency.js"
  },
  "engines": {
    "bun": ">=1.4.1"
  },
  "dependencies": {
    "@elysiajs/cors": "1.4.2",
    "@socket.io/bun-engine": "0.1.2",
    "dotenv": "^17.2.2",
    "elysia": "1.4.30",
    "redis": "^6.2.1",
    "socket.io": "4.8.3",
    "zca-js": "2.1.2"
  }
}
